# ExtractorX Screenshots

Portfolio captures for **D-GITALCODE ExtractorX v2.0.0**.

| File | View |
|------|------|
| `dashboard.png` | Dashboard — stats and format chart |
| `extract-workflow.png` | Extract Images — file queue ready |
| `extract-progress.png` | Extract Images — batch in progress |
| `results-output.png` | Reports — thumbnail gallery |
| `history.png` | History — past extraction runs |
| `settings.png` | Settings — theme, output, reports |
| `output-structure.png` | Output folder structure preview |

## Regenerate

```bash
python scripts/capture_portfolio_screenshots.py
```

Requires Windows with a visible desktop session and installed dependencies.
